# Cloudflare credentials used by scripts/fetch-analytics.mjs
# Do NOT commit real values — set these in GitHub:
#   Settings → Secrets and variables → Actions → New repository secret
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
